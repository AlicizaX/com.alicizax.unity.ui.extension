#if INPUTSYSTEM_SUPPORT && UX_NAVIGATION
namespace UnityEngine.UI
{
    public enum UXInputMode : byte
    {
        Pointer = 0,
        Keyboard = 1,
        Gamepad = 2,
        Touch = 3
    }
}
#endif
